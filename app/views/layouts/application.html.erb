<!DOCTYPE html>
<html>
    <head>
        <title>Animeter</title>
        <meta name="viewport" content="width=device-width,initial-scale=1">
        <link rel="stylesheet" href="./style.css">
        <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@100;300;400;500;700;900&display=swap" rel="stylesheet">

        <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
        <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
        <%=csrf_meta_tags%>
    </head>
    <body>
        <header>
            <div id="header-inner">
                <% if logged_in? %>
                    <h1>
                        <%= link_to current_user do%><%= image_tag 'ロゴ３.png' , id: "header-logo"%><%end%>
                    </h1>
                <%else%>
                    <h1>
                        <%= link_to "/" do%><%= image_tag 'ロゴ３.png' , id: "header-logo"%><%end%>
                    </h1>
                <%end%>
                
                <nav id="header-nav">
                    <ul id="header-ul">
                        <% if logged_in? %>
                            <li id="header-li"><%= link_to "マイページ", current_user , class: "btn btn--orange"%></li>
                            <li id="header-li">
                                <input type="checkbox" id="cp_navimenuid">
                                <label class="menu" for="cp_navimenuid">
                                    <div class="menubar">
                                        <span class="bar"></span>
                                        <span class="bar"></span>
                                        <span class="bar"></span>
                                    </div>
                                    <ul>
                                        <li><%= link_to "ユーザー検索","/users/form"%></li>
                                        <li><%= link_to "アニメ検索","/works/form"%></li>
                                        <li><%= link_to "ログアウト", logout_path, method:"delete" %></li>
                                    </ul>
                                </label>
                            </li>
                        <% else %>
                            <li id="header-li"><%= link_to "新規登録", signup_path, class: "btn btn--orange" , id: "header-li"%></li>
                            <li id="header-li"><%= link_to "ログイン", login_path, class: "btn btn--orange" , id: "header-li"%></li>
                        <% end %>
                    </ul>
                </nav>
            </div>
        </header>
        <% flash.each do |message_type, message| %>
            <div class="alert alert-<%= message_type %>"><%= message %></div>
        <% end %>
        <%= yield %>
        <footer>
            <div id="footer-pc">
                <p>©︎2021Animeter</p>
            </div>
            <div id="footer-sp">
                <div class="footer-sp-inner">
                    
                </div>
            </div>
        </footer>

        <%= debug(params) if Rails.env.development? %>

    </body>
</html>

